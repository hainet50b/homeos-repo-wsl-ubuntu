#!/usr/bin/env sh
set -eu
for agent in $(printf '%s' '{{agents}}' | tr ',' ' '); do
    gh skill list --agent "$agent" --scope user --json skillName,path --jq '.[] | select(.skillName == "{{skill}}") | .path' \
        | while IFS= read -r path; do rm -rf "$path"; done
done
