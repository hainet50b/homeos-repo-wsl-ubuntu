#!/usr/bin/env sh
set -eu
for agent in $(printf '%s' '{{agents}}' | tr ',' ' '); do
    aws agent-toolkit remove-skill --skill-name {{skill}} --agent "$agent" --region us-east-1 --no-sign-request
done
